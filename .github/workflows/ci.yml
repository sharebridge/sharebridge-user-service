name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore SharingBridge.UserService.sln

      - name: Build
        run: dotnet build SharingBridge.UserService.sln -c Release --no-restore

      - name: Test
        run: dotnet test SharingBridge.UserService.sln -c Release --no-build
