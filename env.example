PORT=8081

# Local: copy to .env — loaded automatically on `npm start` (dotenv).
# Render: set the same keys in the dashboard (no .env file on the host).

# PostgreSQL (required — same DATABASE_URL as integration-service)
# Local example: postgresql://sharingbridge:sharingbridge@localhost:5433/sharingbridge
DATABASE_URL=postgresql://sharingbridge:sharingbridge@localhost:5432/sharingbridge
# Schema + Render setup: https://github.com/sharingbridge/sharingbridge/blob/main/configuration/database.md

# Must match sharingbridge-integration-service (HS256 JWT)
AUTH_TOKEN_SECRET=change-me-use-render-generated-secret
AUTH_TOKEN_ISSUER=sharingbridge-user-service
AUTH_TOKEN_AUDIENCE=sharingbridge-clients
AUTH_TOKEN_TTL_SECONDS=3600

# Browser dashboard origin (NOT the API URL). Required for Google sign-in + API calls from the web app.
# Local dev (Vite http://localhost:5173): use the line below in this file only — not on Render.
# Render: set WEB_CORS_ORIGINS in the dashboard to https://<your-static-site>.onrender.com on BOTH
#         user-service and integration-service (comma-add localhost only if you need both).
WEB_CORS_ORIGINS=http://localhost:5173

# Google Sign-In (OAuth client IDs from Google Cloud Console)
GOOGLE_CLIENT_ID_WEB=your-web-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_ID_ANDROID=your-android-client-id.apps.googleusercontent.com
# Optional shorthand when one ID is shared in dev:
# GOOGLE_CLIENT_ID=...

# Logging: error | warn (default) | info | debug — warn = issues only at startup
LOG_LEVEL=warn

# Dev JWT for scripts/tests (not an HTTP endpoint): node scripts/mint-dev-jwt.mjs <user_id> [role]
