# Node.js MVP (archived)

Previous implementation of `sharingbridge-user-service`. Kept for rollback and behaviour reference while the **ASP.NET Core** app is the deployed runtime.

Do not deploy from this folder unless rolling back intentionally.

```bash
cd legacy-node
npm install
npm test
npm start
```
